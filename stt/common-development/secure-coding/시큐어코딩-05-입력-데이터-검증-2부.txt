 시큐어 코딩 5강 강의 시작하겠습니다. 시큐어 코딩 5강에서는 시큐어 코딩 기법에 대해서 계속해서 설명드리겠습니다. 입력 데이터 표현 및 검증에서 나머지 5개에 해당하는 부분에 대해서 이야기를 할 것입니다. 먼저 CSRF입니다. CSRF의 취약점 같은 경우에는 공격자가 이것도 웹서버에 어떤 게시글을 등록을 하는 것인데요. 등록을 할 때 어떤 원래라면 이미지를 등록을 했을 때 웹서버에 이미지가 등록이 될 때는 이미지를 실제로 어떤 게시글에 직접 올라가는 것이 아니라 이미지를 웹서버에 저장을 하고 나서 웹서버에는 웹 게시글에는 그 이미지의 경로가 들어가게 됩니다. 그래서 사용자가 그 게시글을 들어가게 되면 게시글에 써져 있는 이미지의 경로가 어디다라는 것을 보고서 사용자의 컴퓨터에서는 이미지 경로에 해당하는 곳에 어떤 요청을 보내게 되는데요. 근데 이것을 악용해가지고 이미지 태그에 있는 소스에 해당하는 부분에다가 어떤 악의적인 URL을 넣어주게 된다면 나중에 다른 사용자가 그 게시글을 페이지에 요청을 했을 때 그 CSRF의 스크립트에 포함된 이런 악의적인 페이지에 대한 그런 요청이 가게 될 수가 있습니다. 그래서 이 CSRF 같은 경우에도 잔고에서 많이 늘 워낙 유명한 취약점이다 보니까 보안 기능이 설정이 되어 있는데요. 근데 여기에서 잔고의 미들웨어 CSRF를 설정해주는 어떤 세팅이라든지 혹은 CSRF를 제거, CSRF를 기능을 해제해주는 어떤 데코레이터를 사용을 하게 되면 물론 이것을 이제 여러분들이 처음부터 개발할 때는 이런 기능들을 해제하는 기능 같은 경우에는 쓰지 않을 거겠지만 이런 것들이 이제 GitHub에서 보통 소스를 다운받아가지고 사용하는 경우에는 존재할 수 있기 때문에 이런 데코레이터나 아니면은 세팅점 파이에 있는 보안 기능을 제거한 것을 그대로 사용을 하시게 되면은 앞서 말씀드린 것과 같은 CSRF 취약점이 발생할 수 있습니다. 그래서 어떤 코드를 받아와가지고 직접 사용을 할 때라든지 아니면은 실제로 코드를 작성할 때 이런 보안 기능을 여기 있었던 이 보안 기능을 실제로 활성화 해주셔야 되고 그리고 아니면 여기 CSRF.exem 그 데코레이터도 이제 CSRF 기능을 해제하는 것은 사용하지 않도록 해주어야 합니다. 다음은 이제 SSRF입니다. SSRF 같은 경우에는 공격자가 서버로 하여금 서버가 어떤 요청을 공격자가 원하는 요청을 하도록 만드는 것인데요. 일반적으로 공격자에게 허용되지 않은 URL을 서버 같은 경우에는 그 URL에 접속할 수 있는 권한이 존재할 수 있습니다. 그래서 공격자가 어떤 조작된 요청을 보내게 되면은 그 조작된 요청을 웹 서버에서 처리하는 과정에서 어떤 내부 서버에 대한 요청을 보내고 그거에 대한 리스폰스를 공격자에게 노출시키게 되는 그런 상황이 존재할 수 있습니다. 이것이 이제 이런 기법을 보고서 SSRF라고 하는데요. 대표적인 예시로는 이런 것들이 있습니다. 예를 들어서 어떤 사이트에서 커넥트를 해주는 그러니까 그 사이트에서 사이트의 URL에 들어가 있는 이 URL에 그 서버에서 다시 한번 요청을 보내가지고 그 요청을 다시 공격자에게 보여주는 그런 예시가 존재한다고 했을 때 이런 여기 192.168로 시작하는 건 사설 IP잖아요. 그래서 내부망에 존재하는 IP인데 내부망에 있는 member.list.json 뭐 이런 민감정보겠죠. 이런 민감정보들을 공격자 안에 직접 접속할 수가 없으니까 웹서버로 하여금 이런 부분에 한번 요청을 해가지고 그것을 다시 자기한테 표시를 하게 해주는 이런 공격을 사용을 할 수가 있습니다. 그래서 이런 거를 방지를 하기 위해서는 만약에 정말 간단한 예시인데 만약에 서버 측에서 어떤 사용자의 어드레스를 받아가지고 그 어드레스를 기반으로 서버에서 요청을 보내려고 하면 지금 이 사례 같은 경우에는 사용자로부터 받은 어드레스를 그대로 리퀘스트를 보내고 있는데요. 그게 아니라 어떤 어드레스를 받은 것을 그대로 사용을 하는 것이 아니라 사용할 수 있는 그 서버 URL 리스트를 따로 만들어 줘가지고 화이트 리스트 방식으로 예를 들면 여기서는 로컬허스트의 레이티스트 아니면 유저 데이터, 퍼블릭 뭐 이런 식으로 접근할 수 있는 접근할 수 있는 그런 도메인들을 따로 지정을 해가지고 이것에 해당하지 않는 경우에는 모두 다 불가능하도록 다 연결하지 않도록 하는 에러 페이지를 보여주는 그런 식으로 처리를 하는 것이 바람직한 방법입니다. 그리고 이런 식으로 해야만 SSRF로 인한 추약점 발생을 방지할 수 있습니다. 다음은 HTTP 리스폰스 플리팅입니다. 이거 같은 경우에는 이제 브라우저에서 어떤 웹 서버에다가 HTTP 프로토콜을 통해 가지고 어떤 요청을 보내고 나서 다시 리스폰스를 받는 과정에서 중간에 악의적인 입력을 주어 가지고 어떤 의도하지 않았던 그런 그런 리스폰스를 보여주는 것인데요. 이거 같은 경우에는 헤더와 헤더 사이에 존재하는 헤더를 구분해주는 구분자를 추가로 집어 넣어줌으로써 어떤 새로운 헤더를 새로운 헤더를 넣어 줄 수 있게 됩니다. 여기 보시면 컨텐츠 타입에다가 원래 컨텐츠 타입에 들어가야 되는 텍스트랑 HTML뿐만 아니라 뒤에 어떤 어떤 특수문자를 넣고 나서 테스트 스크립트에 해당하는 데이터를 넣어주게 되었는데요. 이 %d, %a가 HTTP 프로토콜에서 헤더를 구분해주는 구분자에 해당하므로 여기 한번 구분해놓고서 구분자가 하나 더 있었기 때문에 또 이것을 새로운 헤더를 인식하게 만들게 됩니다. 그래서 이것에 대해서 어떤 응답이 갔을 때는 이런 식으로 테스트라고 되어 있는 해당하는 부분에는 아예 다른 새로운 헤더 혹은 새로운 컨텐츠로 처리를 하게 되는 것이죠. 좀 더 이해를 쉽게 하기 위해서 좀 더 자세한 예시를 확인해보게 되면요. 여기에서 예를 들어 어떤 요청에서 유저 아이디가 동DD라는 그런 데이터가 들어왔을 때 유저 아이디를 동DD 이렇게 헤더에 집어 넣어주는 예시가 있다고 봅시다. 근데 이때 만약에 저 유저 아이디에 %d, %a에 해당하는 그니까 그 HTTP 프로토콜에서 어떤 구분자에 해당하는 헤더를 구분해주는 구분자에 해당하는 문자를 넣어주게 된다면 여기에 원래 의도하지 않았던 그런 새로운 헤더를 추가로 넣어줄 수 있게 됩니다. 근데 네 이 새로운 헤더를 넣어줌으로써 어떤 이거 리스폰스를 받는 사용자를 하여금 어떤 원래 서버가 의도하지 않았던 그런 데이터를 준다든지 혹은 서버가 의도하지 않았던 헤더를 삽입시키다든지 그런 걸 행위를 할 수 있겠죠. 네 그래서 이거 같은 경우에는 간단한데요. 원래 방금 전과 같이 이제 HTTP 리스폰스 플리팅의 취약점이 발생하기 위해서는 어떤 헤더에 해당하는 값을 처리를 할 때 아무 필터링 없이 처리를 하게 되면 방금 전과 같은 취약점이 발생할 수 있게 되고 좀 전에 취약점 같은 경우에는 이제 구분자에 해당하는 %0b나 %0a에 해당하는 그런 그런 그 문자열들을 모두 제거해준 다음에 그 다음에 처리를 하게 되면은 그럼 좀 전과 같이 이제 구분자를 보고 나서 이것이 원래는 하나의 헤더로 처리가 되었어야 했던 것을 여러 개의 헤더로 처리한다든지 그런 상황이 발생할 수 발생하지 않게 되겠죠. 그래서 넣으면 안 되는 문자열들이 들어왔을 때 그것을 없애주는 작업을 추가로 해주게 되면은 이런 HTTP 리스폰스 플리팅에 대한 취약점을 방지할 수 있습니다. 그리고 다음은 이제 인티저 오버플로우입니다. 인티저 오버플로우는 원래 허용된 범위 이상의 입력값을 주었을 때 어떤 에러가 발생하는 그런 것을 보고 그런 것을 이야기를 하는데요. 예시를 보시겠습니다. 예를 들어서 이제 파이썬에는 이제 numpy라는 패키지가 있습니다. numpy가 이제 행렬을 처리할 때 많이 사용되는 패키지인데요. 이때 데이터 타입이 int64라고 설정을 하게 되면은 int64에 해당하는 범위에 있는 데이터만 처리를 할 수가 있습니다. 만약에 이 int64 이상 int64에서 처리를 하는 데이터를 넘어가는 숫자가 넘어가는 숫자가 데이터로 입력으로 돌아온다든지 혹은 그걸 여기에서 오버플로우를 얘기를 하고 있지만 언더플로우 그 표현할 수 있는 것보다 더 작은 숫자의 입력이 들어왔을 경우에는 그런 경우에는 이제 파이썬이 오류가 나면서 중간에 멈춰버리게 됩니다. 그래서 그런 것들을 방지하기 위해서 뭐 맥시멈 넘버보다 더 높은 경우라든지 맥시멈 넘버보다 더 작은 입력값이 들어왔을 경우에는 뭐 리터널을 통해 가지고 함수가 제대로 어... 함수가 이 numpy에 하나 numpy 함수에 들어가지 못하도록 진입하지 못하도록 어떤 처리를 예외 처리를 해준다든지 혹은 어... 이게 아니라면은 뭐 맥시멈 값보다 맥시멈 값보다 큰 입력값이 들어왔을 경우에는 맥시멈 값에 해당하는 값으로 침환시켜준다든지 그런... 그런 작업을 통하게 되면은 그러면은 이런 함수들을 numpy 함수에 직접 입력값을 넣었을 때 오류가 발생하거나 아니면은 파이썬이 종료되는 그런 행위를 그런 현상을 막을 수가 있습니다. 네 그리고 다음은 format string injection 입니다. 웹서버에서 어떤 문자열을 처리를 할 때 그... 문자열로 들어가는 값이 정상 문자열이 아니라 뭐 파이썬에서 새로 특정한 기능을 하는 문자열들이 들어왔을 경우에는 그런 것들은 예외 처리를 해주는 과정이 필요합니다. 근데 만약에 그런 과정이 없었더라면은 어... 공격자가 파이썬에서 특별한 의미를 갖고 있는 문자열들을 집어넣어가지고 서버에서 원래는 보여지면 안 될 데이터를 공격자에게 보여지는 경우가 존재할 수 있습니다. 예시로 보시겠습니다. 만약에 이제 유서... 사용자가 입력값이 이제 format string에 들어가야 될 텐데요. format string과 user info가 모두 사용자의 입력값으로 받아오게 되는데요. 이 두 입력값이 모두 다 사용자의 입력으로 들어오기 때문에 만약에 이 유저 인포에 어... 저... authenticate 키에 해당하는 그런 키값을 포함하도록 그런 format string을 format string을 설정하고 나서 그리고 유저 인포를 그렇게 적절하게 넣어주게 되면은 이 유저 페이지에 메세지로 들어오는 값을 통해가지고 어떤 서버에 설정되어 있는 원래 뭐 패스워드 키값 아니면 뭐 세션값 등 세션값들이 무엇이었는지 확인을 할 수 있게 됩니다. 네, 그래서 이제 다음과 같이 어... 사용자의 입력값은 어떤 파이썬을 처리할 때 있어가지고 format string format string으로써 사용을 할 수 없게 해야 합니다. 네, 여기서는 이제 유저 네임이 뭐... 중괄호를 해가지고 원래 format string을 앞서 앞에서는 사용자의 입력값으로 받았었는데 그것이 아니라 이제 서버 개발할자가 직접 지정을 해줌으로써 어떤 다른 악의적인 입력값이 들어오지 못하게 하였고요 이거 말고도 이제 파이썬 3.7, 3.6, 3.7, 3.8 이상부터 이제 f string이라는 것이 존재해가지고 그런 f... 그런 f string을 사용하게 되면 앞서 앞서 이 사용자에게 format string을 설정하도록 하지 못하게 해가지고 그런 뭐... 파이썬 파일 안에 있는 특정 민감 정보를 읽는다든지 그런 인젝션 등을 방지할 수가 있습니다. 네, 이상으로 여기까지 해서 5강을 마치겠습니다. 다음 영상에서 만나요! 다음 영상에서 만나요! 다음 영상에서 만나요! 5강을 마치겠습니다. 다음 영상에서 만나요!