 챕터 4 네트워크 계층 챕터 4 네트워크 계층 시작하도록 하겠습니다 저희가 앞단에서 2계층 데이터 링크 레이어 계층을 다뤄봤는데요 그 계층에서는 프레임이라고 하는 단위를 근거리 네트워크에서 한 네트워크의 요소에서 이윤 네트워크 요소로 전달한다는 계층을 이해했습니다 그 계층에서는 맥주소라고 하는 그 계층에서 사용하는 주소를 사용하고요 이제 저희가 다룰 계층은 처음에 5개 계층에서 세번째 계층인 네트워크 계층에 대해서 소개를 드리겠습니다 먼저 이 네트워크 계층은 IP 계층이라고도 하고요 그 전달하는 단위는 데이터그램이라는 표현을 씁니다 아까 데이터 링크 레이어에서 한 네트워크 요소에서 이윤 네트워크 요소로 프레임을 전달한다고 했는데요 이 네트워크 레이어에서는 한 호스트에서 다른 호스트로 데이터가 갈 때 그 데이터가 어떠한 최적의 경로를 선택하고 전송하는지에 대한 내용이라고 이해하시면 됩니다 그래서 쉽게 말해서 라우팅 역할을 수행한다고 이해하시면 됩니다 이 네트워크 계층은 흔히 IP 계층이라고도 한다 했는데요 그래서 이 IP라고 하는 프로토콜을 수행하게 됩니다 이 다음 계층인 트랜스포트 계층, 전송 계층의 세그먼트를 운반하는 역할을 합니다 데이터 링크는 네트워크 레이어의 데이터그램을 프레임으로 전달하는 역할을 하고 이 네트워크 레이어는 상위 계층인 트랜스포트의 세그먼트라는 단위를 운반하는 역할을 하는 것이죠 예시를 한번 들어보겠습니다 두 개의 호스트, 하나의 호스트는 H1이라고 하고 또 다른 하나의 호스트는 H2라고 했을 때 이 H1의 트랜스포트 계층에서 세그먼트를 추출해서 이 H2의 네트워크 계층을 통해서 트랜스포트 계층으로 전달하는 역할을 네트워크 계층은 수행하게 되고요 그래서 여기서 하나만 더 설명을 드리자면 호스트 대 호스트 전달을 담당한다고 이해하시면 될 것 같습니다 아까 데이터 링크는 근거리 네트워크에서 한 네트워크 요소에서 이온 네트워크 요소로 전달하는 역할을 한다 했고 이 네트워크 계층에서는 하나의 호스트 대 호스트로 세그먼트, 전송 계층의 세그먼트를 전송하는 역할 그리고 이후에 설명하겠지만 트랜스포트 계층에서는 프로그램 내 프로그램에 그런 메시지를 전달하는 그런 역할을 한다고 이해하시면 될 것 같습니다 계속 리마인드를 드려야지 여러분들이 좀 쉽게 이해하실 수 있을 것 같아서 자꾸 말씀을 드리고 있고요 그래서 이 네트워크 계층은 데이터 평면과 제어 평면이 두 가지로 나누어서 볼 수 있습니다 데이터 평면은 데이터그램을 전달하는 데 있어서 IP 프로토콜을 사용하고 이런 내용을 다루고요 제어 평면은 전달할 때 어떤 라우팅 알고리즘을 사용하고 어떻게 포워딩을 하는지 이런 내용을 다룹니다 참고로 제 강의에서는 자세한 라우팅 알고리즘이나 제어 평면 내용은 다루지 않습니다 왜냐하면 저희가 실질적으로 패킷을 분석하고 해킹을 하기 위해서 필요한 네트워크 지식은 IP 프로토콜을 이해하는 것으로 네트워크 기초 강의는 준비가 되어 있습니다 이 네트워크 계층에서 제공할 수 있는 서비스는 다음과 같습니다 제공할 수 있는 서비스입니다 첫째로 보장된 전달을 할 수 있습니다 이는 무슨 뜻이냐면 출발지 패킷에서 목적지 도착을 보장하는 그 패킷이 잘 도착했다는 보장을 제공할 수 있고요 그리고 어떤 지연 제한 시간 안에 도착이 된다고 보장을 할 수 있고요 그리고 순서 패킷 전달은 송신된 순서대로 차례대로 도착을 하는 것을 의미합니다 이러한 서비스도 가능하고요 최소 대역 보장, 특정한 속도 이하로 전송 가능한 모든 데이터그램이 전달됨을 보장하는 내용을 설명하고 있고요 그리고 트랜스포트 계층 세그먼트들이 어떤 암호화가 되어 있고 이랬을 때 기밀성을 제공하는 그런 역할도 수행을 하게 됩니다 하지만 인터넷 네트워크 계층에서 제공하는 서비스는 앞서 나와 있던 서비스를 모두 다 반영하진 않고요 인터넷 네트워크 계층, 저희가 지금 사용하는 네트워크에서는 최선형 서비스를 제공하게 됩니다 네트워크 계층에서 이것은 뭐냐면 패킷을 보내는 순서대로 수신됨을 보장하지 않고요 그리고 목적지까지 패킷이 정손됨을 보장하지 않습니다 그리고 종단 시스템 간의 지연이 보장되지 않고요 그리고 보장된 최소 대역 복은 없습니다 되게 간단한 구조를 가지고 있죠 앞서 설명한 서비스에 비해서 그래서 이런 것을 어떻게 하면 더 좋게 할까 다양한 서비스 모델들이 정의되고 구현되고 있습니다 하지만 최선형 서비스, 인터넷 네트워크 계층은 이미 이러한 최선형 서비스를 이용해도 충분히 좋다고 입증이 되어 있습니다 왜냐하면 이렇게 단순할수록 실시간 어플리케이션이나 어떤 성능이 중요시되는 그런 네트워크에서 충분히 좋은 성능을 낼 수 있기 때문이죠 다음은 인터넷 프로토콜에 대해서 IP 프로토콜에 대해서 한번 이해를 해보겠습니다 지금 보이시는 화면은 IP 버전 4 데이터그램 포맷입니다 보시면 20바이트의 헤더, 보통 20바이트고 여기에 옵션이 있거나 하면 조금 가변적일 수 있습니다 하지만 보통은 20바이트 헤더를 가지고요 지금 보시면 네트워크 계층에서 사용하는 주소인 IP 주소가 여기 지금 소스 IP, 데스트 IP가 중간에 있고요 그리고 그 뒤에 세그먼트를 데이터 필드에 담아서 전송하는 역할이 되죠 그리고 이 전체가 데이터그램이라고 캡슐화가 돼서 데이터그램이라고 표현할 수 있습니다 여기서 좀 더 살펴볼 만한 것은 체크섬이라는 게 있죠 그래서 체크섬은 비트가 뭔가 오류가 발생했거나 이런 것을 탐지할 수 있는 것을 말하고요 그리고 Type of Service 이 부분은 어떤 트랜스포트의 계층이 어떤 서비스를 가지고 있는지 그 세그먼트가 어떤 서비스인지를 명시하는 것이라고 이해하시면 될 것 같습니다 그리고 여기 데이터그램의 랭스도 기입이 되어 있고요 모든 호스트랑 라우터는 IP 데이터그램을 송수신할 수 있습니다 따라서 이 IP 주소라는 것을 갖도록 요구가 되고 있는데요 그래서 지금 여기 보시는 사진처럼 이런 저희가 흔히 쓰는 컴퓨터들 호스트들이 어떤 특정한 IP들을 가지고 있게 됩니다 이 IP 주소는 여러분들도 많이 보셨겠지만 32비트의 길이를 가지고 있는데요 그래서 각 바이트는 점으로 구분되는 형식으로 되어 있습니다 서브넷은 무엇이냐면 지금 보시면 이 사진, 이 그림 같은 경우에는 3개의 서브넷으로 분리가 되어 있고 이것들이 모두 이어져 있는데요 서브넷은 IP 네트워크를 더 작고 더 관리하기 쉬운 부분으로 나눈 것이라고 할 수 있습니다 이 IP 버전4 주소체계의 클래스 주소체계라는 것을 이해할 필요가 있는데요 필요한 호스트 IP 개수에 따라서 서브넷에 필요한 호스트 IP 개수에 따라서 네트워크의 크기를 다르게 할당하는 것입니다 그래서 IP 주소가 32비트가 있지만 여기서 네트워크 영역이 있고 호스트 영역을 따로 분리해서 이 네트워크 영역에서는 이만큼 호스트 영역의 개수만큼 네트워크 IP 주소를 사용할 수 있게 되는 것이죠 그래서 지금 A, B, C, D, E 이렇게 클래스로 나오는데 보통 이제 저희가 가정용에서 사용하는 클래스가 네트워크에서 사용하는 클래스가 C 클래스이죠 그래서 이 C 클래스 같은 경우에는 192.0.0에서 23.2525라는 네트워크 영역을 갖게 되고요 그래서 저희가 보통 공유기를 사용하면 192.168.0.1 이런 주소를 갖게 되는 것이 이 C 클래스 서브넷을 사용하기 때문입니다 그래서 이 C 클래스 같은 경우에는 호스트 어드레스가 0부터 255번까지 그 개수만큼만 IP 주소를 할당할 수 있게 됩니다 저희가 대학교나 기업이나 이런 데는 B 클래스 조금 더 호스트 영역의 큰 클래스 주소 체계를 사용해서 할당을 진행을 하게 됩니다 그래서 B 클래스는 C 클래스보다 더 많은 호스트 어드레스를 할당할 수 있죠 아까 맥 브로드캐스트와 같게 IP에서도 브로드캐스트 주소가 있는데요 255.255.255.255라는 주소를 가지고 있고 목적 주소가 이 주소면 이 서브넷에 있는 모든 호스트들에게 전달을 하겠다는 뜻을 갖게 됩니다 그리고 이 NOT 이라고 하는 네트워크 어드레스 트랜슬레이션 이것은 저희 IP 버전4가 IP 할당에 대한 문제가 많이 있습니다 그래서 하나의 공용 IP를 사용해서 프라이빗 네트워크 내에 여러 개의 프라이빗 IP 주소와 통신할 수 있게 하는 가상 IP 체제라고 할 수 있습니다 그리고 DHCP는 좀 다른 얘기인데 저희가 아까 말했듯이 그런 클래스 네트워크가 있을 때 IP 주소를 수동으로 하나하나 다 할당을 해줄 수도 있지만 DHCP를 이용해서 동적으로 자동으로 IP 주소를 랜덤하게 할당해주는 그런 프로토콜이고요 이것을 이용해서 IP 버전4의 IP 고갈 문제들을 어느 정도 해결할 수 있게 되었습니다 이 IP 버전4 주소 공간 고갈 문제는 꽤나 오래전부터 논의가 되어왔습니다 첫 설계 때는 이렇게 고갈 될지 몰랐다가 점점 사용을 하다 보니까 고갈될게 보였던 거죠 실제로 그래서 IP 버전6라고 하는 주소 체계가 꽤나 오래전부터 개발이 되어왔는데요 중요한 변화는 아까 IP 버전4는 32비트이기 때문에 표현할 수 있는 IP 주소가 한정이 되어 있었습니다 하지만 IP 버전6는 확장된 주소를 사용해서 총 128비트의 크기를 갖고요 그리고 좀 더 헤더를 간소화하고 그리고 흐름 레이빌링이라고 하는 패킷의 플로우를 조금 조절할 수 있는 같은 흐름이면 동일하게 처리한다던가 이런 중요한 변화가 있는데요 사실 이 IP 버전6는 아직까지는 수용은 되고 있지만 널리 사용되고 있지는 않습니다 그래서 이 IP 버전6 데이터그램의 구조는 지금 아래 보이시는 사진과 같고요 참고하시고 넘어가시면 좋을 것 같습니다 이렇게 네트워크 레이어 라우팅 역할을 하고 그리고 호스트 대 호스트 대 호스트 대 호스트 로 그 세그먼트를 운반하는 데이터그램에 대해서 저희가 한번 이해를 해 봤습니다 좋습니다 그렇기 때문에ayer 4моцы imagining