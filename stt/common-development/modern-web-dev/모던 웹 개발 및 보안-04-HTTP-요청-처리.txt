 이번 시간에는 HTTP 요청 처리 실습 한번 같이 해보겠습니다 아마 이전 웹패킹 시간에 배우셨을 거예요 HTTP에서 데이터를 주고받는 방법에 대해서요 HTTP 요청에 따라 여러 메소드들이 있었죠 GET 메소드, POST 메소드, 그리고 데이터를 줄 때 GET에서는 QUAR STRING으로 주고 아니면 파라미터로 주고 바디 데이터로 주는 등 데이터를 주고받는 그런 프로토콜들이 있었는데 이번 시간에는 익스프레스 모듈에서 어떻게 HTTP 요청들을 처리하는지 그리고 데이터를 주고받는지에 대해서 배워볼 겁니다 기본적으로 익스프레스는요 바디를 파싱하지 못해요 그렇기 때문에 보시는 것처럼 익스프레스 제이슨이나 URL 인코디드 같은 미들웨어를 사용을 해서 바디 데이터들을 파싱할 수가 있습니다 물론 GET 쪽에 있는 URL QUARLY STRING은 잘 파싱이 되고요 그러면 실습 진행해 보도록 하겠습니다 먼저 네 번째 실습이니까 HTTP, 리퀘스트라는 폴더를 하나 만들어주고 여기에 똑같이 이니셜라이즈를 또 해줍니다 네 이니셜라이즈를 해주고 너무 빨리 넘겼나요? 이니셜라이즈 해주고 이제 DOCKBIS 코드를 통해서 코드를 열어주고 인덱스.js를 작성을 해줄 건데요 자 먼저 이제 이번 시간에 알아볼 건 쿼리, 파라미터, 리퀘스트 객체가 어떤 데이터들을 다룰 수 있는지를 배워볼 건데 일단 지난 시간에 작성했던 간단한 웹앱 한번 다시 작성을 해 볼게요 익스프레스 모듈을 먼저 불러야겠죠 익스프레스 모듈을 불러오고 그 익스프레스 모듈을 인스턴스화 해주고 앱에 담아 놓고 그 다음에 이제 라우팅 처리를 해볼게요 여기에 또 리퀘스트 리스폰스 콜백 받아서 처음에 이렇게 작성을 했었죠 그리고 처음에 리슨을 통해서 여기에 포트를 넣어줬었는데 이번에는 그냥 여기다 바로 넣어볼게요 여기에 뭐 리스닝 온 코러스트 3000 이렇게 코드를 작성하고 일단 지금 기본 터미널이 파워셀로 돼 있어 가지고 WS로 바꿔줬고요 여기서 노드 인덱스.js를 통해서 서버를 고정시켜 줄 수 있습니다 지금 제가 새로운 폴더를 만들었는데 익스프레스를 설치하지 않아서 에러가 났는데 익스프레스 다시 설치해 주고요 그 다음에 인덱스.js를 통해서 서버를 구동시켜 주고 이번에 접속을 해볼게요 지금 엣지로 나오는데 이거를 크롬에서 정상적으로 웹웹이 동작을 하고요 여기서 이제 저희가 뭐 여기서 get method 이런 식으로 이런 식으로 q는 뭐 1, 2, 3 이런 식으로 데이터를 받아왔었잖아요 그걸 이제 익스프레스에서 어떻게 받는지를 실습을 같이 해 볼 거예요 먼저 이 get method로 query string 받는 법부터 한번 배워볼게요 이 데이터에 들어오는 리퀘스트 먼저 콘솔 로그로 한번 찍어 보겠습니다 일단 먼저 제가 말씀을 드리자면 리퀘스트의 query라는 값이 이 query string을 가져올 수 있는데 리퀘스트라는 객체를 콘솔 로그로 찍어 보고 q의 1, 2, 3이라는 값을 주어서 요청을 줘보면 이제 오른쪽에 보시는 것 같이 이렇게 데이터 리퀘스트라는 객체가 가지고 있는 데이터들이 뜨는데요 음... 여기에 여기 보시면 이제 false URL 그 밑에 이제 query라는 값에 q는 1, 2, 3이라는 값이 뜨고 이 밑에 false URL 말고 그냥 query에 q라는 key 값에 1, 2, 3이 등록이 되어 있습니다 아 그러면 저희는 여기서 알아낼 수 있죠 아 리퀘스트 객체의 query라는 값이 요렇게 query string을 받는 값이구나 아 방법이구나 해서 콘솔 로그로 찍어 볼게요 다시 query를 콘솔 로그 찍게 하고 리퀘스트를 주면 q는 1, 2, 3이 뜨겠고 q에 이제 q 값을 또 찍어 주면 여기서 이제 1, 2, 3 값이 이렇게 정상적으로 출력이 되겠죠 이거를 뭐 예를 들어서 q가 아니라 뭐 유저네임 이런 식으로 받아서 if request query 유저네임이 뭐 어드민이면 콘솔 아니 뭐 res send를 hello admin 이라고 줘라 이런 로직을 또 구현을 할 수 있을 거예요 그죠? 당연히 굉장히 보안성으로 취약한 코드지만 여기에 query string을 유저네임에 어드민을 주게 되면 hello admin이라는 이 조건문을 통과해서 이렇게 query string을 다뤄볼 수 있습니다 네 바로 다음으로 두 번째 실습 진행해 보도록 하겠습니다 좀 전에 이제 query string을 다루는 방법을 배웠는데요 이번에는 리퀘스트에 있는 다른 그 속성인 params에 대해서 배워 볼 거예요 이번엔 query가 아니라 params에 대해서 배워 볼 건데 뭐 예를 들면 이런 게 있어요 아마 REST API에 대해서 혹시 배워 보신 분들이 있으실 텐데 있으실 수도 있는데 어 좀 뭐 방법론 중에 하나거든요 웹 애플리케이션을 개발하기 위한 방법론 중 하나 중 뭐 예를 들어서 users라는 라우팅에 get 메소드를 주면 유저의 데이터를 가져오고 post 하면은 유저의 뭔가 데이터를 주고 put 하면 유저를 추가하고 delete 하면 유저를 삭제하는 이런 방법론이 있어요 이때 많이 사용을 하는데 예를 들어서 이런 식으로 REST 풀 하게 구현을 하면 여기에 뭐 delete 1 하면서 유저의 1번 인덱스를 가진 유저를 삭제한다 뭐 2번 유저를 추가한다 3번 유저의 데이터에 뭘 추가한다 뭐 10번 유저의 데이터를 가져온다 이런 식으로 구현하는 걸 이제 REST 풀 하게 구현을 한다 라고 하는데 REST 풀 API 이게 요즘 되게 많이 사용하고 있는 웹 앱 개발 패러다임인데 요거를 사용을 하려면 이제 요 값을 받아야 되잖아요 요 값을 받는 방법이 바로 params입니다 빠르게 실습을 한번 같이 해볼게요 params를 받는 방법은 똑같이 이제 get 메소드로 한번 예습을 받아 볼게요 뭐 예를 들어서 유저라는 라우터에 제가 뒤에 있는 id 값을 받아 본다고 해볼게요 뭐 예를 들어서 유저 뒤에 인덱스가 뭐 13번 유저를 가져온다 라고 예를 들어 봤을 때 여기에 13을 가져야 되잖아요 근데 예를 들어서 유저가 100명이 있다고 하면 저희가 뭐 이거를 뭐 이렇게 100개를 만들 순 없잖아요 이렇게 1, 2, 3, 4 이렇게 100개를 만들 순 없으니까 이거를 동적으로 받긴 해야 되는데 이걸 받는 방법은 바로 이렇게 받습니다 이 콜론을 주고 id를 이런 식으로 주면 그리고 뒤에 콜백함수 연해주고 여기서 이제 콘솔로그 리퀘스트 한번 받아 볼게요 자 서버 다시 실행시켜주고 여기서 이제 유저의 뒤에 뭐 예를 들어서 13을 넣어주면 지금 행이 걸려 있는데 이 행이 걸려 있는 이유는 우리가 리스폰스를 안 줬죠 그렇기 때문인데 제가 서버에서는 아마 정상적으로 콘솔로그가 실행이 됐을 겁니다 자 이 파란즈라는 속성에 바로 우리가 들어간 그러니까 id 13이라는 값이 들어가 있네요 그러면 리퀘스트에는 파란즈라는 값이 이 파라미터들을 가져올 수 있는 방법이구나 라고 이해하시면 될 것 같아요 다시 실행해 보면 이제 id 13이라는 값이 정상적으로 들어왔죠 그쵸? 이렇게 파라미터 값 받아보는 방법 배워봤습니다 다음 마지막 실습입니다 http 리퀘스트에는 이제 포스데이터 흔히 포스데이터라고 말하는 바디 값을 또 받는 방법이 있어요 근데 제가 아까 말씀드렸다시피 이 바디 값은요 익스프레스에서 기본적으로는 파싱하지 못해요 그렇기 때문에 이제 그 바디 값을 파싱하는 미들웨어를 또 사용을 해줘야 되는데요 보통 노드.js에서는 데이터를 주고 받을 때 노드.js 뿐만 아니라 요즘 json 데이터를 많이 주고 받잖아요 그래서 보시는 첫 번째 예시처럼 express.json이라는 모듈을 미들웨어로 등록을 해주면 그 다음부터는 json 값을 정상적으로 파싱해줄 수 있습니다 뭐 예전처럼 url 인코딩 된 url 인코디드 모듈도 당연히 있고 이걸 임포트 해주면 보시는 것처럼 제 포스데이터들을 정상적으로 받고 줄 수가 있게 됩니다 바로 실습 해보겠습니다 자 바디 값을 받는 방법인데요 먼저 첫 번째로 json 데이터를 한번 받아 볼게요 json 데이터를 받기 위해선 자 설명드린 대로 이제 express에 있는 json이라는 이 미들웨어를 사용해주면 됩니다 이 미들웨어를 이렇게 등록을 하면 되고요 그리고 한 여기 쯤에 이제 뭐 앱.post 값을 라우팅 해주는데 유저에 이제 뭐 유저 그냥 이렇게 한번 써볼까요 해서 콜백 값을 작성해주고 여기서 이제 포스트 값을 받았을 때 또 다시 한번 콘솔 로그를 찍어봅시다 여기서 음 일단 send를 뭐 post request at user 이런 식으로 send를 한번 주고요 이 포스트 데이터를 보내기 위해서는 뭐 아마 이전 시간에 뭐 여러가지 방법으로 실습을 해보셨을 텐데 뭐 html에서 폼데이터를 보내셔도 되고 컬을 간단하게 이용을 해도 되고요 뭐 proxy 툴을 이용을 해도 됩니다 근데 저 이번에 컬을 한번 이용을 해볼게요 이렇게 포스트 데이터를 user라는 라우팅에 포스트 데이터가 오면 이 로즙을 실행하게끔 작성을 해두고 이제 한번 포스트 데이터를 보내볼게요 포스트 데이터를 보내는 방법은 컬 로 보내는 방법은 이제 컬 url 한번 적어주고요 먼저 헤더가 있어야 돼요 헤더가 그 컨텐츠 컨텐츠 타입이라는 헤더가 있어야 되는데 이게 이렇게 application slash json이어야 얘가 http 요청이 json 요청이구나 라고 알아들어요 그리고 나서 데이터를 줘야 이게 json 인지 압니다 자 json 데이터가 예를 들어서 user id 뭐 어드민이란 아이디를 가진 바이 뭐 이런 데이터를 이런 json 데이터들을 이런 json 데이터를 user로 한번 보내볼게요 그리고 뒤에 url 써주고요 3000 써주고 저희가 라우팅을 user로 해놨기 때문에 user로 보내야겠죠 이렇게 보내주면 네 포스트 리퀘스트의 user 하고 우리가 작성한 이 로직이 실행된 걸 확인할 수 있고 리퀘스트가 다시 콘솔 로그에 찍혔어요 위쪽에 보면은 이제 파람즈 없고 쿨이 없는데 우리가 보낸 값이 여기 이제 작성돼 있는 걸 볼 수 있죠 리퀘스트의 바디에 우리가 입력한 이 json 데이터들이 들어가있다 라고 저희는 확인할 수 있고 아 이 익스페스 앱은 뭐 바디로 포스트 뭐 포스트 뿐만 아니라 여러 가지 그 htb 요청의 바디 부분을 봐줄 수 있구나 그리고 json 데이터를 받기 위해선 이 이 익스페스 제이슨이라는 이 미들웨어가 필요하고 그리고 이 컨텐츠 타입이라는 헤더가 반드시 필요하구나 를 이제 알고 넘어가면 될 것 같아요 만약에 이 json 이라는 미들웨어가 없거나 저 헤더가 없으면요 자 데이터를 보냈을 때도 이렇게 undefined 작성이 됩니다 이 json 데이터를 파싱하지 못한다는 뜻이 되겠죠 반대로 이제 헤더가 헤더가 만약에 없으면 어떨까요 헤더가 이제 json 데이터가 아니면 받을 것 같긴 한데 헤더가 없어도 이 바디 데이터를 받지 못한다 그러니까 이 json 미들웨어와 이 json 헤더가 반드시 컨텐츠 타입이 json이라는 이 요청이 반드시 필요하다 라고 이해하시면 될 것 같아요 그래서 이 제이슨 미들웨어와 이 제이슨 헤더가 반드시 제이슨이 반드시 반드시 반드시 제이슨이라는 요청이 반드시 필요하다 라고 이해하시면 될 것 같아요 근데 저희 아마 이전 시간에 json 데이터 말고 보통 url encoded 된 데이터들도 보내셨을 거예요 그건 또 어떻게 받냐 그거를 위해서는 이제 그 데이터를 받기 위해서는 익스프레스에 url encoded 라는 미들웨어가 또 있어요 요걸 써주면 이제 바디 값을 또 받아올 수 있는데 이번에 이번에 json 데이터가 아니라 id는 admin이라는 값을 그대로 던져볼게요 id가 admin이라는 값을 그대로 call d id admin host 3000 user 이렇게 작성해서 보내면 url encoded 값도 정상적으로 id는 admin 값도 정상적으로 받아서 출력해주는 것을 확인하실 수 있습니다 이렇게 해서 htp 요청 처리 실습 마무리 하도록 하겠습니다