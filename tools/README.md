# Local Tools

이 폴더의 스크립트는 강의 사이트 생성 전 로컬 작업에만 사용한다. GitHub Pages에서 직접 불러오는 런타임 파일이 아니다.

- `screenshot.py`: 원본 영상에서 일정 간격으로 캡처 이미지를 추출한다.
- `stt.py`: Whisper로 영상 또는 음성 파일을 텍스트로 변환한다.

공개 페이지에 노출할 캡처 이미지는 `videos/`가 아니라 `screenshots/` 아래에 정리한다.
